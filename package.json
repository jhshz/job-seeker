{
  "name": "jobbama",
  "version": "1.0.0",
  "private": true,
  "description": "Jobbama (جاب با ما) - MERN stack monorepo application",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "dev": "concurrently \"yarn dev:backend\" \"yarn dev:frontend\"",
    "dev:backend": "yarn workspace @jobbama/backend dev",
    "dev:frontend": "yarn workspace @jobbama/frontend dev",
    "build": "yarn workspaces foreach -A run build",
    "build:backend": "yarn workspace @jobbama/backend build",
    "build:frontend": "yarn workspace @jobbama/frontend build",
    "lint": "yarn workspaces foreach -A run lint",
    "lint:backend": "yarn workspace @jobbama/backend lint",
    "lint:frontend": "yarn workspace @jobbama/frontend lint",
    "clean": "yarn workspaces foreach -A run clean && rm -rf node_modules",
    "outdated": "yarn outdated",
    "audit": "yarn audit",
    "audit:fix": "yarn audit --fix"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.54.0",
    "@typescript-eslint/parser": "^8.54.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.39.2",
    "husky": "^6.0.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=4.0.0"
  },
  "packageManager": "yarn@4.12.0"
}
