// packages/backend/src/docs/swagger.ts
/**
 * Swagger/OpenAPI stub.
 * Install swagger-jsdoc + swagger-ui-express and mount here.
 * Example:
 *   const swaggerUi = require('swagger-ui-express');
 *   const swaggerJsdoc = require('swagger-jsdoc');
 *   const options = { definition: { openapi: '3.0.0', info: { title: 'Job Seeker API', version: '1.0.0' } }, apis: ['./src/routes/*.ts'] };
 *   app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerJsdoc(options)));
 */
export const swaggerStub = true;
